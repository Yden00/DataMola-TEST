import { ColoringChipsDirective } from './coloring-chips.directive';

describe('ColoringChipsDirective', () => {
  it('should create an instance', () => {
    const directive = new ColoringChipsDirective();
    expect(directive).toBeTruthy();
  });
});
